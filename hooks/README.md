# Hook Templates

These are hook prompt templates, not shell hooks.

Use them to wire agent sessions into predictable maintenance behavior:

- `session-start.md`: recover context before work starts
- `post-git-push.md`: sync the project after code lands
- `fix-to-rule.md`: convert repeated pain into reusable policy

